# slices.yaml

# Slice per il traffico Gaming
gaming:
  flows:
    - {src: g1, dst: gs}
  capacity_pct: 60
  priority: 50

# Slice per il traffico Work
work:
  flows:
    - {src: h1, dst: h5}
  capacity_pct: 20
  priority: 50

# Slice per il traffico Emergency
emergency:
  flows:
    - {src: ps, dst: h3}
  capacity_pct: 20
  priority: 100

# Nuova slice per testare i conflitti di banda
video:
  flows:
    - {src: h2, dst: gs}
  capacity_pct: 50
  priority: 30