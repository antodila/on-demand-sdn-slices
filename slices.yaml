# slices.yaml

# Slice per il traffico Gaming
gaming:
  flows:
    - {src: g1, dst: gs}
  capacity_pct: 60

# Slice per il traffico Work
work:
  flows:
# [SOLUZIONE] Correggi l'indentazione. Il trattino deve essere allineato con 'flows'.
    - {src: h1, dst: h5}
  capacity_pct: 20

# Slice per il traffico Emergency (esempio per test futuri)
emergency:
  flows:
    - {src: ps, dst: h3}
  capacity_pct: 20